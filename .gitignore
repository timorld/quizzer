# Server runtime data — never overwrite on the server
server/data/

# Remote-player test — the player/ subfolder is its own git repo
# (pushed to github.com/timorld/quizzer-player), don't double-track here
test-remote-player/player/

# Secrets — env files with API keys, never commit
server/.env
.env

# Dependencies
server/node_modules/
node_modules/

# Local testing files
testing/

# TLS certificates — sensitive, keep off GitHub
TLS certificates/

# Large binary / debugging artifacts not needed in repo
assets/ALM settings issue/
assets/SCORMs for example/
assets/GRAPHICS/illustrations.ai
assets/Documentation/

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.swp

# Claude memory (local only)
.claude/
